version: '3.8'

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nhiot_dashboard
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - HOST=0.0.0.0
      - PYTHONUNBUFFERED=1
    restart: always
